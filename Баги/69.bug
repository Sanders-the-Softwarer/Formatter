-- ƒаже если длина выражени€ позвол€ет выт€нуть его в одну строку,
-- строчный комментарий должен помешать этому и выражение должно
-- быть отформатировано в несколько строк

i := case -- ≈сли изменилс€ маппинг корр., то проставл€ем пользовател€, изменившего запись
         when v_is_cr = 'Y' then
             ui_rm_utils.get_user
         else
             null
     end;