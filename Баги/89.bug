-- Условие if не должно заезжать в конец строчного комментария,
-- а сам комментарий должен выравниваться по условию, а не по if-у

begin
if
            --не корректно когда что-то не указано
             p_edi_sysrowid is null
             or p_edi_barcode is null
then 
  null;
end;