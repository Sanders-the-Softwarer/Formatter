-- При вытягивании выражения в строку строчный комментарий к одному из его элементов не должен пропадать
-- Потом в связи со сменой реализации строчные комментарии стали конвертироваться в скобочные

create or replace force view v_cc_outdoc as
    select
        decode
            (
                dummy, 
                'X', -- Конечно, в dual оно и лежит
                1, 
                2 -- Но мы на всякий случай проверим
            )
    from
        dual