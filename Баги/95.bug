-- Из-за комментария в одну строку нормально не записывается,
-- поэтому надо генерировать многострочный вариант, даже если
-- вписывается в правила однострочного

sm_arch_utils.generate_history
            (
                t_number_table(p_hs_code),      -- так как hs_code ключевое поле в составнок ключа (hs_code, product_region)
                v_table_code
            );