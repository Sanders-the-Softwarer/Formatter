create or replace package x is

    -- Заголовочный комментарий
    -- Должен быть отдельно
    -- И от верхней, и от нижних строк

    /* Комментарий перед функцией должен быть к ней прижат */
    function y return varchar2;

    /* И отделён пустой строкой от следующей */
    function z return number;

end x;
/
