-- При отмене форматирования текст должен выводиться без "улучшающих модификаций"
-- типа вставки in в параметры, замены default на := итп.

/* -- vvv -- */
procedure x(a integer, b integer default 0) as
begin
end;
/* -- ^^^ -- */