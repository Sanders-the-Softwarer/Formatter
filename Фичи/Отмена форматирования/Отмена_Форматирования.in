-- Текст, заключённый между комментариями с vvv и ^^^, должен выдаваться в результат отформатированным так, как в оригинале

begin
    if a > b then
        b := a;
    else
        a := b;
    end if;
                        /* -- vvv -- Отсюда не форматируем */
      if a > 
    b then b := a
    ; else a 
      := b; end if;
                        /* -- ^^^ -- Отсюда снова форматируем */
    if a > b then
        b := a;
    else
        a := b;
    end if;
end;