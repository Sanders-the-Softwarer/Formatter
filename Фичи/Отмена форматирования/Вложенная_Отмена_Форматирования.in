-- Вложенные отмены форматирования действуют от начала первого до конца последнего, как парные скобки

begin
    if a > b then
        b := a;
    else
        a := b;
    end if;
                        /* -- vvv -- Отсюда не форматируем */
      if a > 
    b then b := a
                        /* -- vvv -- Отсюда тем более не форматируем */
    ; else a 
      := b; end if;
                        /* -- ^^^ -- Отсюда пробуем снова форматировать, но не получается */
    if a > b then
        b 
            := 
        a
            ;
                        /* -- ^^^ -- А вот отсюда - получается */
    else
        a := b;
    end if;
end;