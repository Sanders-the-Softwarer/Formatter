select * from dual where dummy = 'X' -- Этот комментарий не должен приводить к переносу 'X' на следующую строку из-за того, что при печати строка оказывается длиннее лимита символов однострочного выражения