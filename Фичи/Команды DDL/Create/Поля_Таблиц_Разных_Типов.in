-- ѕол€ таблиц могут содержать разные типы, default и not null
create table x
    (
        id integer not null, 
        di varchar2(2000 char) default 'abc' || 'de', 
        ididi date default sysdate not null
    )
