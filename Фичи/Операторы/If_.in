-- Форматирование if / then / elsif / else
begin
    if a = b then 
        null; 
    end if;
    if a = b then 
        null; 
    else 
        raise; 
    end if;
    if a = b then 
        null; 
    elsif c = d then 
        return; 
    elsif e > f then 
        proc; 
    else 
        raise; 
    end if;
end;

-- При многострочном условии then должно выводиться под if
if p_manual_data_source_code.table_of_something(i).value
   not in
   (v_data_source_fs1_code, v_data_source_fs2_code, v_data_source_order_code) 
then
    return v_result;
else
    return null;
end if;